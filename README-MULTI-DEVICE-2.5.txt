KING TECH SUBSCRIPTION MANAGER 2.5 — MULTI-DEVICE JOIN

NEW:
- Separate Migrate This Device and Join Shared Database actions.
- Original device uploads and verifies its records.
- Additional devices download only and cannot overwrite the shared sheet with blank data.
- Joined devices auto-refresh every 20 seconds and upload later edits.
- Local checkpoint and pre-join backup created before replacing any device copy.
- Clear status labels: Migrated, Joined, Not connected, Local.
- Existing backup export/import and safe empty-sheet protection retained.
- Manual account availability and slot calculations retained.

SETUP:
1. Update Code.gs and deploy a New version in Apps Script.
2. Replace website files and push to GitHub.
3. Original device: Migrate This Device.
4. Other devices: Join Shared Database.
